1. Which file is latest?
2. Which file is production-ready?
3. What risk does this create?
Answers -
1. The file customer_cleaning_final_v2 appears to be the latest version as per the naming conventions used by the author.
2. The file customer_cleaning_final_v2 can be trusted as it reflects the latest state of the customer_cleaning file. However all files have a single line so it can't be fully acclaimed that it is the final version of the code. 
3. If two engineers edited different copies then it would become ambiguous to find out which copy is the latest and updated copy that contains all the up to date changes. If they would have updated the same line then the final version would rewrite one copy without flagging.
